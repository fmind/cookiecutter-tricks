tricks:
- watchdog.tricks.LoggerTrick:
    patterns: ["*.*"]
- watchdog.tricks.ShellCommandTrick:
    patterns: ["*.*"]
    # wait_for_process: true
    drop_during_process: true
    shell_command: "echo 'file changed!'"
